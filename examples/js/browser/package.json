{
  "name": "aoo-browser",
  "version": "0.1.0",
  "description": "A simple AOO Receiver in the Browser",
  "license": "MIT",
  "author": "Davide Gagliardi",
  "type": "module",
  "main": "index.js",
  "workspaces": [
    "./client",
    "./server"
  ],
  "scripts": {
    "build": "npm run build --workspace client && npm run build --workspace server",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "start": "npm run build && concurrently \"node server/dist/index.js\" \"npm run preview --workspace client\""
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
